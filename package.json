{
  "name": "gowithtami",
  "version": "1.0.0",
  "description": "Your personalized AI travel planner. Tami gets to know you — and sends you where you belong.",
  "main": "index.js",
  "scripts": {
    "dev": "lite-server --baseDir=\"public\"",
    "watch": "sass --watch src/scss:public/assets/css",
    "start": "concurrently \"npm run watch\" \"npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Tamires Segata",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "lite-server": "^2.6.1",
    "sass": "^1.89.2"
  },
  "dependencies": {
    "animejs": "^4.0.2",
    "lightbox2": "^2.11.5"
  }
}
